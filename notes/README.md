# 발견 노트

코어를 만들다 **눈으로 확인된 것**을 한 건씩 남긴다. 아카이브의 최소 단위이자 콘텐츠의 원재료.

한 항목은 네 가지를 갖는다:

1. **표기법** — 무엇을 겹쳤는가 (뷰어의 공유 버튼이 그대로 뽑아 준다)
2. **링크** — 그 화면을 다시 여는 주소
3. **그림** — `shots/` 의 스크린샷
4. **수치** — 눈이 아니라 숫자로 확인한 것

뷰어의 `⧉ 공유`(단축키 `C`)가 1·2를 한 번에 복사한다. 그대로 붙여넣고 3·4를 채우면 된다.
